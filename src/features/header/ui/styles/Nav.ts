import styled from 'styled-components/macro';

export const Nav = styled.div`
    position: absolute;
    top: left;
    z-index: 20;
    display: flex;
    justify-content: space-between;
    width: 100%;
    padding: 20px;
`;
