import styled from 'styled-components/macro';

export const Nav = styled.div`
    width: 100%;
    position: absolute;
    top: left;
    z-index: 20;
    padding: 20px;
    display: flex;
    justify-content: space-between;
`;
