import styled from 'styled-components';

const Nav = styled.div`
	position: absolute;
	z-index: 20;
	top: left;

	display: flex;
	justify-content: space-between;

	width: 100%;
	padding: 20px;
`;

export { Nav };
