import { createLocales } from '@lib/locales';

const locales = createLocales<'<%= name %>'>()({
    /**
     * write a map of translations here.
     * locales see in "src/shared/config/[lang].json"
     */
});

export { locales };
